from __future__ import annotations


def load_prompt(prompt_id: str, version: str) -> str:
    _ = (prompt_id, version)
    raise NotImplementedError("Prompt registry is not implemented in this skeleton.")
