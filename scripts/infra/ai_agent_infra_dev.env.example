AZURE_LOCATION=eastus
KEY_VAULT_NAME=<your-key-vault-name>
LOG_ANALYTICS_WORKSPACE_ID=/subscriptions/<sub>/resourceGroups/<rg>/providers/Microsoft.OperationalInsights/workspaces/<workspace>
ALERT_EMAIL=alerts@example.com
PE_SUBNET_NAME=<private-endpoint-subnet-name>

# Used only when PostgreSQL server is created for the first time.
LANGFUSE_PG_ADMIN_USER=pgadmin
LANGFUSE_PG_ADMIN_PASSWORD=<strong-password>
